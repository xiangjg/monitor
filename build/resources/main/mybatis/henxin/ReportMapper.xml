<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ReportMapper">
	<resultMap type="Report" id="reportMap">
		<id column="id" property="id"/>
		<result column="type" property="type"/>
		<result column="address" property="address"/>
		<result column="bank" property="bank"/>
		<result column="client" property="client"/>
	</resultMap>
	
	<!-- 判断用户名和密码 -->
	<select id="getReportInfo" parameterType="Report" resultMap="reportMap">
		select * from hx_report
		where 1=1
		<if test="type!=null">
		and type = #{type}
		</if>
		<if test="address!=null">
			and address = #{address}
		</if>
		<if test="bank!=null">
			and bank = #{bank}
		</if>
		<if test="client!=null">
			and client = #{client}
		</if>
	</select>
	<!-- 修改
	<update id="editU" parameterType="pd" flushCache="false">
		update  SYS_USER
		set USERNAME				= #{USERNAME},
		NAME 					= #{NAME},
		ROLE_ID 				= #{ROLE_ID},
		BZ						= #{BZ},
		EMAIL					= #{EMAIL},
		NUMBER					= #{NUMBER},
		PHONE					= #{PHONE}
		<if test="PASSWORD != null and PASSWORD != ''">
			,PASSWORD				= #{PASSWORD}
		</if>
		where
		USER_ID = #{USER_ID}
	</update> -->
	<!-- 新增 -->
	<insert id="save" parameterType="Report" flushCache="false">
		insert into hx_report (
		id,
		type,
		address,
		bank,
		client
		) values (
		#{id},
		#{type},
		#{address},
		#{bank},
		#{client}
		)
	</insert>
</mapper>